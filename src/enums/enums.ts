export enum Colors {
    WHITE = "white",
    BLACK = "black",
}

export enum Panels {
    History = "history",
    Bot = "chooseBot",
    Menu = "menu",
    Board = "board",
    Friends = "friends",
}

export enum gameEndStatuses {
    Win = "You Win",
    Lose = "You Lose",
    Draw = "Draw",
}
